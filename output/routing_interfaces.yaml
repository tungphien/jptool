---

Granular_tests:

    # Owner: nhuu
    hello:
        common_variables:
                 device: device
        steps:
                 1:
                     add_timestamp:
                         run_event:
                             keyword: On CLI
                             device: device
                             command: 'set cli timestamp'
                         unique_id: 1
                 2:
                     clear_log_messages:
                         run_event:
                             keyword: On CLI
                             device: device
                             command: 'clear log messages'
                         unique_id: 2